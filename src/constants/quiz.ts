export enum EExerciseType {
  INSET = "inset",
  SELECT = "select",
  MULTIPLE_SELECT = "multiple-select",
}

export enum ECourseRole {
  USER = "user",
  EDITOR = "editor",
  REVIEWER = "reviewer",
}
